graphql:
    packages:
        - "nl.nlportal"

nl-portal:
    haalcentraal:
        url: #filled in test
        apiKey: "some-api-key"

spring:
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: http://localhost:8082/auth/realms/nlportal