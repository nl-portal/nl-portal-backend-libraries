graphql:
    packages:
        - "nl.nlportal"

nl-portal:
    config:
        haalcentraal:
            brp:
                enabled: true
                properties:
                    url: #filled in test
                    apiKey: "some-api-key"

spring:
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: http://localhost:8082/auth/realms/nlportal