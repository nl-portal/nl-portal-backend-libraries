query {
    getProducten(productName: "erfpacht") {
            number
            size
            totalElements
            numberOfElements
            content {
                id,
                naam
            }
        }
}
