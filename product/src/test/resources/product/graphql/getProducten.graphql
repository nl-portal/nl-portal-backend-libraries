query {
    getProducten(productName: "erfpacht") {
            number
            size
            totalElements
            numberOfElements
            content {
                naam
            }
        }
}
