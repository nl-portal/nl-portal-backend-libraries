query {
    getProductType(productName: "erfpacht") {
            id,
            naam,
            omschrijving,
            zaaktypen,
            beslistabelMappings,
            prefillMappings
        }
}
